@model IEnumerable<AssetManager.Models.ProjectRule>

@{
    ViewBag.Title = "Project Rules";
}

<script>
    $("#locBar").append("<span>Project Rules</span>");
</script>

<h2>Project Rules</h2>

<p>
    @Html.ActionLink("Create New Rule", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Project.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Project.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { projectId=item.ProjectId, userId = item.UserId }) |
            <!--@@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |-->
            @Html.ActionLink("Delete", "Delete", new { projectId = item.ProjectId, userId = item.UserId })
        </td>
    </tr>
}

</table>
